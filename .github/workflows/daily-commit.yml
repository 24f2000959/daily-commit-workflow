jobs:
  commit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Commit step - robinrao198@gmail.com
        run: |
          echo "TDSFU Daily run: $(date -u) TDSFU" >> daily-log.txt

      # 💡 IMPORTANT: Update this step to use your required student email
      - name: Git Commit and Push
        run: |
          # 🚨 FIX: Using the required student email for verification
          git config user.name "Robin Yadav"  # Use your name
          git config user.email "24f2000959@ds.study.iitm.ac.in" 
          git add daily-log.txt
          git commit -m "Auto commit at $(date -u)" || echo "No changes to commit"
          git push
